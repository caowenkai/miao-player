# 喵音乐播放器 (Miao Player) 🎵

一个完整的在线音乐网站，支持H5/Web应用端和后台管理系统。

## 项目结构

```
miao-player/
├── frontend-app/    # H5/Web 应用端（Vue3 + Vite）
├── frontend-admin/  # 后台管理端（Vue3 + Vite + Element Plus）
├── backend/         # 后端API服务（Node.js + Express + SQLite）
├── uploads/         # 音乐文件存储目录
├── docs/            # 部署文档
└── README.md
```

## 功能特性

### 应用端
- 🎵 音乐播放器（播放/暂停、进度控制）
- 📝 歌词同步显示
- 📱 歌单列表管理
- 🎨 H5优化的界面设计

### 后台管理
- 📤 音乐文件上传
- 🎧 在线试听
- 📋 歌单创建与管理
- 🔍 歌曲搜索与添加

## 快速开始

### 环境要求
- Node.js 16+
- npm 或 yarn

### 开发环境

#### 方式一：一键启动（推荐）

```bash
bash start-dev.sh
```

该脚本会自动：
- 检查并安装所有依赖
- 创建必要目录
- 同时启动所有三个服务

#### 方式二：手动启动

1. 安装依赖
```bash
# 安装后端依赖
cd backend && npm install

# 安装前端应用依赖
cd ../frontend-app && npm install

# 安装后台管理依赖
cd ../frontend-admin && npm install
```

2. 启动开发服务器
```bash
# 启动后端服务（默认端口：3000）
cd backend && npm run dev

# 启动前端应用（默认端口：5173）
cd frontend-app && npm run dev

# 启动后台管理（默认端口：5174）
cd frontend-admin && npm run dev
```

### 生产部署

#### 方式一：一键部署

```bash
bash deploy.sh
```

#### 方式二：详细部署

- **CentOS/TencentOS 服务器**: [CentOS 部署指南](./docs/DEPLOYMENT_CENTOS.md) ⭐
- **Ubuntu/Debian 服务器**: [通用部署文档](./docs/DEPLOYMENT.md)

## 技术栈

- **前端框架**: Vue 3 + Composition API
- **构建工具**: Vite
- **UI组件**: Vant (移动端) / Element Plus (管理端)
- **后端框架**: Express
- **数据库**: SQLite
- **文件上传**: Multer
- **进程管理**: PM2

## 开发者

开发时间：2025年11月

## License

MIT
